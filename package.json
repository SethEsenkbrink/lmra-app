{
  "name": "lmra-app",
  "version": "8.0.1",
  "description": "LMRA Pro v8.0 - Sentinel (Offline First)",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Geen build stap nodig voor Vanilla JS'",
    "clean": "rimraf node_modules package-lock.json",
    "security-check": "npm audit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "netlify-cli": "latest",
    "rimraf": "^6.0.0"
  },
  "overrides": {
    "jws": "^3.2.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}