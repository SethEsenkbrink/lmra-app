{
  "name": "lmra-app",
  "version": "8.2.0",
  "description": "LMRA Pro v8.2 - Sentinel (Netlify Identity)",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Geen build stap nodig voor Vanilla JS'",
    "clean": "rimraf node_modules package-lock.json",
    "security-check": "npm audit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "netlify-cli": "latest",
    "rimraf": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}